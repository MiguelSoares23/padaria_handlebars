<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de clientes</title>
</head>
<body>
    <h1>Lista de clientes</h1>

    <ul>
        {{#each clientes}}
        <li>
            <a href="/clientes/{{id}}">nome: {{nome}}</a>|
            <a href="/clientes/{{id}}/editar">Editar</a>

            <form action="/clientes/excluir/{{id}}" method="post" style="display:inline;">
                <button type="submit" onclick="return confirm('Confirma exclusão?')">excluir</button>
            </form>
        </li>
        {{/each}}
    </ul>

    <h4><a href="/homeClientes">Voltar</a></h4>
</body>
</html>