<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Equipamentos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

<div class="container mt-5">

    <h1 class="text-center mb-4">Lista de Equipamentos</h1>

    <ul class="list-group shadow">

        {{#each equipamentos}}
        <li class="list-group-item d-flex justify-content-between align-items-center">

            <div>
                <strong>{{nome}}</strong> — <span class="text-muted">{{estado}}</span>
            </div>

            <div class="d-flex gap-2">

                <a href="/equipamentos/{{id}}" class="btn btn-sm btn-info text-white">Detalhes</a>

                <a href="/equipamentos/{{id}}/editar" class="btn btn-sm btn-warning">Editar</a>

                <form action="/equipamentos/excluir/{{id}}" method="post"
                      onsubmit="return confirm('Confirma exclusão?')">
                    <button class="btn btn-sm btn-danger">Excluir</button>
                </form>

            </div>
        </li>
        {{/each}}

    </ul>

    <a href="/homeEquipamentos" class="btn btn-link mt-3">Voltar</a>

</div>

</body>
</html>
