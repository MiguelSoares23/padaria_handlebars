<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Fornecedores</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

<div class="container mt-5">

    <h1 class="text-center mb-4">Listar fornecedores</h1>

    <ul class="list-group">
        {{#each fornecedores}}
        <li class="list-group-item d-flex justify-content-between align-items-center">

            <span>
                <a href="/fornecedores/{{id}}" class="fw-bold text-decoration-none">
                    {{nome}}
                </a>
                <br>
                <small class="text-muted">Telefone: {{telefone}}</small>
            </span>

            <span>
                <a href="/fornecedores/{{id}}/editar" class="btn btn-warning btn-sm">Editar</a>

                <form action="/fornecedores/excluir/{{id}}" method="post" style="display:inline;">
                    <button type="submit" class="btn btn-danger btn-sm"
                        onclick="return confirm('Confirma exclusão?')">
                        Excluir
                    </button>
                </form>
            </span>

        </li>
        {{/each}}
    </ul>

    <div class="text-center mt-3">
        <a href="/homeFornecedores" class="btn btn-secondary">Voltar</a>
    </div>
</div>

</body>
</html>
